export interface Particle {
  x: number;
  y: number;
  xs: number;
  ys: number;
  age: number;
  maxAge: number;
  reachMaxAge: boolean;
}

export interface Dimensions {
  w: number;
  h: number;
}
